{
  "buildCommand": "npm run build --workspace=@drink-ux/shared && npm run build --workspace=@drink-ux/mobile",
  "outputDirectory": "packages/mobile/dist",
  "installCommand": "npm ci",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
