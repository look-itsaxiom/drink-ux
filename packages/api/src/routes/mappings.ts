/**
 * Item Mappings Routes
 * STUB: Implementation pending - tests first!
 */

import { Router } from 'express';
import { ItemMappingService } from '../services/ItemMappingService';

export function createMappingsRouter(_service: ItemMappingService): Router {
  const router = Router();
  
  // TODO: Implement routes (TDD green phase)
  // Routes not yet implemented - tests should fail
  
  return router;
}
