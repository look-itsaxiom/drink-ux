{
  "buildCommand": "npm run build --workspace=@drink-ux/shared && npm run build --workspace=@drink-ux/admin",
  "outputDirectory": "packages/admin/dist",
  "installCommand": "npm ci",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
