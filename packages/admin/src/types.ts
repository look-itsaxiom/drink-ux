/**
 * Local types for admin app
 * These are specific to the admin portal and not shared across packages
 */

export enum POSProvider {
  SQUARE = "square",
  TOAST = "toast",
  CLOVER = "clover",
}
