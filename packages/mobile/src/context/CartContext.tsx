/**
 * CartContext
 * Re-exports from useCart hook for consistency
 *
 * The cart context is already implemented in the useCart hook.
 * This file provides a consistent export pattern with other contexts.
 */

export { CartProvider, useCart, type CartItem, type CartContextValue, type CustomerInfo } from '../hooks/useCart';
